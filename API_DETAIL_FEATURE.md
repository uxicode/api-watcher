# 📝 API 상세 정보 기능 추가

## ✅ 구현 완료

API 목록에서 각 엔드포인트를 클릭하면 Parameters, Request Body, Responses 정보를 확인할 수 있습니다!

---

## 🎯 기능 요약

### 확장 가능한 API 항목
- **클릭으로 확장**: 각 API 항목을 클릭하면 상세 정보 표시
- **표시 정보**:
  - Parameters (쿼리, 경로, 헤더 파라미터)
  - Request Body (요청 본문 스키마)
  - Responses (응답 상태 코드 및 스키마)

---

## 🎨 UI 변경사항

### Before (간단한 목록)
```
┌─────────────────────────────────────────────┐
│ [GET]  /api/users           사용자 목록 조회 │
└─────────────────────────────────────────────┘
```

### After (클릭으로 확장)

**기본 상태:**
```
┌─────────────────────────────────────────────┐
│ [GET]  /api/users           사용자 목록 조회 ▶│
└─────────────────────────────────────────────┘
```

**클릭 후 확장:**
```
┌─────────────────────────────────────────────┐
│ [GET]  /api/users           사용자 목록 조회 ▼│
├─────────────────────────────────────────────┤
│ Parameters                                   │
│   page (query) required 페이지 번호          │
│   limit (query) 페이지당 항목 수             │
│                                              │
│ Request Body                                 │
│   {                                          │
│     "content": { ... }                       │
│   }                                          │
│                                              │
│ Responses                                    │
│   [200] OK                                   │
│         Successful response                  │
│         { "content": { ... } }               │
│   [400] Bad Request                          │
│         Invalid parameters                   │
└─────────────────────────────────────────────┘
```

---

## 💻 구현 상세

### 1. **데이터 구조 확장**

```typescript
interface ApiEndpoint {
  method: string
  path: string
  summary?: string
  requestBody?: any          // ✅ 추가
  responses?: Record<string, any>  // ✅ 추가
  parameters?: any[]         // ✅ 추가
}
```

### 2. **상태 관리**

```typescript
const expandedApiIndex = ref<number | null>(null)

function toggleApiDetail(index: number) {
  if (expandedApiIndex.value === index) {
    expandedApiIndex.value = null  // 닫기
  } else {
    expandedApiIndex.value = index  // 열기
  }
}
```

### 3. **헬퍼 함수**

```typescript
// JSON 포맷팅
function formatJson(obj: any): string {
  return JSON.stringify(obj, null, 2)
}

// HTTP 상태 코드 클래스
function getStatusClass(statusCode: string): string {
  const code = parseInt(statusCode)
  if (code >= 200 && code < 300) return 'status-success'   // 2xx: 성공
  if (code >= 300 && code < 400) return 'status-redirect'  // 3xx: 리다이렉트
  if (code >= 400 && code < 500) return 'status-client-error'  // 4xx: 클라이언트 에러
  if (code >= 500) return 'status-server-error'  // 5xx: 서버 에러
  return ''
}
```

---

## 📋 표시 정보

### 1. **Parameters**

```
Parameters
┌─────────────────────────────────────────┐
│ id (path) required 사용자 ID            │
│ page (query) 페이지 번호                │
│ limit (query) 페이지당 항목 수          │
│ Authorization (header) required 인증 토큰│
└─────────────────────────────────────────┘
```

**표시 항목:**
- 파라미터 이름 (굵게)
- 위치 (path, query, header, cookie)
- Required 여부 (빨간색)
- 설명

### 2. **Request Body**

```
Request Body
┌─────────────────────────────────────────┐
│ {                                       │
│   "content": {                          │
│     "application/json": {               │
│       "schema": { ... }                 │
│     }                                   │
│   }                                     │
│ }                                       │
└─────────────────────────────────────────┘
```

**특징:**
- 다크 테마 코드 블록
- JSON 형식으로 포맷팅
- 스크롤 가능 (최대 300px)

### 3. **Responses**

```
Responses
┌─────────────────────────────────────────┐
│ [200]  Successful response              │
│        { "data": [ ... ] }              │
│                                         │
│ [400]  Bad Request                      │
│        Invalid parameters               │
│                                         │
│ [500]  Internal Server Error            │
│        Server error occurred            │
└─────────────────────────────────────────┘
```

**색상 구분:**
- **2xx (성공)**: 초록색
- **3xx (리다이렉트)**: 보라색
- **4xx (클라이언트 에러)**: 노란색
- **5xx (서버 에러)**: 빨간색

---

## 🎨 스타일링

### 1. **확장 애니메이션**

```scss
@keyframes slideDown {
  from {
    opacity: 0;
    max-height: 0;
  }
  to {
    opacity: 1;
    max-height: 500px;
  }
}
```

**효과:**
- 부드러운 펼쳐짐
- 0.2초 transition

### 2. **코드 블록 (다크 테마)**

```scss
.code-block {
  background: #1e1e1e;       // VS Code 다크 배경
  color: #d4d4d4;            // 밝은 텍스트
  font-family: 'Monaco', 'Menlo', monospace;
  padding: 8px;
  border-radius: 4px;
  overflow: auto;
}
```

### 3. **상태 코드 배지**

```scss
// 2xx - 성공
.status-success {
  background: #d1fae5;
  color: #065f46;
}

// 4xx - 클라이언트 에러
.status-client-error {
  background: #fef3c7;
  color: #92400e;
}

// 5xx - 서버 에러
.status-server-error {
  background: #fee2e2;
  color: #991b1b;
}
```

---

## 🚀 사용 방법

### 1. **API 항목 클릭**

```
[클릭] → [GET] /api/users  사용자 목록 조회  ▶
                                          ↓
          [GET] /api/users  사용자 목록 조회  ▼
          ├─ Parameters
          ├─ Request Body
          └─ Responses
```

### 2. **다시 클릭하면 닫힘**

```
[클릭] → [GET] /api/users  사용자 목록 조회  ▼
                                          ↓
         [GET] /api/users  사용자 목록 조회  ▶
```

### 3. **다른 항목 클릭**

```
기존 항목은 자동으로 닫히고
새로운 항목이 열립니다
```

---

## 📊 화면 예시

### GET 엔드포인트

```
┌─────────────────────────────────────────────────┐
│ [GET] /api/users/{id}  사용자 상세 조회        ▼│
├─────────────────────────────────────────────────┤
│ Parameters                                       │
│   id (path) required 사용자 ID                  │
│                                                  │
│ Responses                                        │
│   [200] OK                                       │
│         {                                        │
│           "id": 1,                               │
│           "name": "John Doe",                    │
│           "email": "john@example.com"            │
│         }                                        │
│   [404] Not Found                                │
│         User not found                           │
└─────────────────────────────────────────────────┘
```

### POST 엔드포인트

```
┌─────────────────────────────────────────────────┐
│ [POST] /api/users  새 사용자 생성               ▼│
├─────────────────────────────────────────────────┤
│ Request Body                                     │
│   {                                              │
│     "name": "string",                            │
│     "email": "string",                           │
│     "password": "string"                         │
│   }                                              │
│                                                  │
│ Responses                                        │
│   [201] Created                                  │
│         User successfully created                │
│   [400] Bad Request                              │
│         Invalid input data                       │
└─────────────────────────────────────────────────┘
```

---

## 💡 장점

### 1. **완전한 API 문서**
- Parameters, Request, Response 한눈에 확인
- Swagger UI와 유사한 경험

### 2. **공간 효율성**
- 필요한 정보만 확장하여 표시
- 스크롤 양 최소화

### 3. **개발자 친화적**
- 코드 블록 스타일로 명확하게 표시
- HTTP 상태 코드 색상 구분

### 4. **빠른 참조**
- 프로젝트 상세 페이지에서 즉시 확인
- 별도 Swagger UI 열 필요 없음

---

## 🔍 정보 표시 우선순위

### 항목이 존재할 때만 표시

```typescript
// Parameters가 있을 때만
<div v-if="endpoint.parameters && endpoint.parameters.length > 0">
  <!-- Parameters 섹션 -->
</div>

// Request Body가 있을 때만
<div v-if="endpoint.requestBody">
  <!-- Request Body 섹션 -->
</div>

// Responses가 있을 때만
<div v-if="endpoint.responses">
  <!-- Responses 섹션 -->
</div>
```

---

## 📱 반응형 지원

### 데스크톱
- 2열 레이아웃 (상태 코드 + 내용)
- 코드 블록 가독성 최적화

### 모바일
- 1열 레이아웃 (세로 스택)
- 작은 폰트 크기
- 터치로 확장/축소

---

## 🎯 실제 데이터 예시

### Petstore API - POST /pet

```
[POST] /pet  Add a new pet to the store         ▼
├─────────────────────────────────────────────────┤
│ Request Body                                     │
│   {                                              │
│     "id": 0,                                     │
│     "name": "doggie",                            │
│     "category": {                                │
│       "id": 0,                                   │
│       "name": "Dogs"                             │
│     },                                           │
│     "photoUrls": ["string"],                     │
│     "tags": [                                    │
│       { "id": 0, "name": "string" }              │
│     ],                                           │
│     "status": "available"                        │
│   }                                              │
│                                                  │
│ Responses                                        │
│   [200] Successful operation                     │
│   [405] Invalid input                            │
└─────────────────────────────────────────────────┘
```

---

## 🎉 완성!

이제 API 목록에서:
- ✅ **기본 정보** - 메서드, 경로, 설명
- ✅ **Parameters** - 쿼리, 경로, 헤더 파라미터 (NEW!)
- ✅ **Request Body** - 요청 스키마 (NEW!)
- ✅ **Responses** - 응답 상태 및 스키마 (NEW!)

모든 정보를 확인할 수 있습니다! 🚀

**페이지를 새로고침하고 API 항목을 클릭해보세요!**
