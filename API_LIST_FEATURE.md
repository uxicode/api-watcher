# 📋 API 목록 기능 추가

## ✅ 구현 완료

프로젝트 상세 화면에서 현재 등록된 API 엔드포인트 목록을 확인할 수 있게 되었습니다!

---

## 🎯 기능 요약

### 프로젝트 정보 영역에 API 목록 추가
- **위치**: `/projects/:id` (프로젝트 상세보기)
- **표시 내용**: 
  - HTTP Method (GET, POST, PUT, DELETE 등)
  - API 경로 (Path)
  - API 설명 (Summary)
- **정렬**: 경로 → 메서드 순서

---

## 📊 화면 구성

### 프로젝트 상세 페이지 레이아웃

```
┌─────────────────────────────────────────────────┐
│ ← 홈          프로젝트명                         │
│              swagger URL                        │
│                        [체크] [수정] [삭제]      │
├─────────────────────────────────────────────────┤
│ 프로젝트 정보                                    │
│ ┌────────────┬────────────┬────────────┬───────┐│
│ │마지막 체크 │스냅샷 수   │최신 버전   │총 API││
│ │2분 전      │3개         │v1.0.0     │15개  ││
│ └────────────┴────────────┴────────────┴───────┘│
├─────────────────────────────────────────────────┤
│ API 목록                          총 15개 엔드포인트│
│ ┌─────────────────────────────────────────────┐ │
│ │ [GET]  /api/users           사용자 조회     │ │
│ │ [POST] /api/users           사용자 생성     │ │
│ │ [GET]  /api/users/{id}      사용자 상세     │ │
│ │ [PUT]  /api/users/{id}      사용자 수정     │ │
│ │ [DELETE] /api/users/{id}    사용자 삭제     │ │
│ │ ...                                         │ │
│ └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│ 변경 내역                                        │
│ (기존 diff 카드들...)                           │
└─────────────────────────────────────────────────┘
```

---

## 🎨 UI 특징

### 1. **HTTP Method 색상 구분**

```scss
GET    → 파란색 (#dbeafe / #1e40af)
POST   → 초록색 (#d1fae5 / #065f46)
PUT    → 노란색 (#fef3c7 / #92400e)
PATCH  → 보라색 (#e0e7ff / #3730a3)
DELETE → 빨간색 (#fee2e2 / #991b1b)
```

### 2. **API 목록 표시**

```
┌─────────────────────────────────────────┐
│ [GET]  /api/products                    │
│        모든 상품 목록 조회               │
├─────────────────────────────────────────┤
│ [POST] /api/products                    │
│        새 상품 생성                      │
└─────────────────────────────────────────┘
```

**구성 요소:**
- **메서드 뱃지**: HTTP 메서드 (색상 구분)
- **경로**: Monospace 폰트로 명확하게 표시
- **설명**: 회색 텍스트로 간략하게 표시

### 3. **스크롤 가능**
- 최대 높이: 500px (모바일: 400px)
- 엔드포인트가 많을 경우 스크롤바 표시
- 커스텀 스크롤바 디자인

### 4. **호버 효과**
- 마우스 오버 시 배경 변경
- 약간 우측으로 이동 (4px)
- 부드러운 트랜지션

---

## 💻 구현 상세

### 1. **데이터 파싱**

```typescript
const apiEndpoints = computed<ApiEndpoint[]>(() => {
  const latestSnapshot = snapshots.value[0]
  if (!latestSnapshot) return []

  try {
    const swaggerData = JSON.parse(latestSnapshot.data)
    const endpoints: ApiEndpoint[] = []

    if (swaggerData.paths) {
      for (const [path, methods] of Object.entries(swaggerData.paths)) {
        for (const [method, details] of Object.entries(methods)) {
          if (['get', 'post', 'put', 'delete', 'patch', 'options', 'head']
              .includes(method.toLowerCase())) {
            endpoints.push({
              method,
              path,
              summary: details?.summary || details?.description || ''
            })
          }
        }
      }
    }

    // 경로 → 메서드 순으로 정렬
    return endpoints.sort((a, b) => {
      if (a.path !== b.path) {
        return a.path.localeCompare(b.path)
      }
      const methodOrder = { get: 1, post: 2, put: 3, patch: 4, delete: 5 }
      return (methodOrder[a.method] || 99) - (methodOrder[b.method] || 99)
    })
  } catch (error) {
    console.error('Failed to parse swagger data:', error)
    return []
  }
})
```

### 2. **인터페이스**

```typescript
interface ApiEndpoint {
  method: string      // HTTP 메서드
  path: string        // API 경로
  summary?: string    // 설명 (선택)
}
```

### 3. **템플릿 구조**

```vue
<div class="api-list-section">
  <div class="section-header">
    <h2>API 목록</h2>
    <span class="api-count">총 {{ apiEndpoints.length }}개 엔드포인트</span>
  </div>
  
  <div v-if="apiEndpoints.length === 0" class="empty-state">
    아직 스냅샷이 없습니다. "지금 체크하기"를 눌러 API 정보를 가져오세요.
  </div>
  
  <div v-else class="api-list">
    <div v-for="endpoint in apiEndpoints" class="api-item">
      <div class="api-method" :class="`method-${endpoint.method}`">
        {{ endpoint.method.toUpperCase() }}
      </div>
      <div class="api-info">
        <div class="api-path">{{ endpoint.path }}</div>
        <div class="api-summary">{{ endpoint.summary }}</div>
      </div>
    </div>
  </div>
</div>
```

---

## 🚀 사용 방법

### 1. **프로젝트 체크**

먼저 프로젝트를 체크하여 스냅샷을 생성합니다:

1. 프로젝트 상세 화면 접속
2. **"지금 체크하기"** 버튼 클릭
3. ✅ Swagger 문서 로드 완료

### 2. **API 목록 확인**

스냅샷이 생성되면 자동으로 API 목록이 표시됩니다:

```
API 목록                          총 15개 엔드포인트
┌─────────────────────────────────────────────┐
│ [GET]  /api/users           사용자 목록 조회 │
│ [POST] /api/users           새 사용자 생성   │
│ [GET]  /api/users/{id}      사용자 상세 조회 │
│ [PUT]  /api/users/{id}      사용자 정보 수정 │
│ [DELETE] /api/users/{id}    사용자 삭제     │
└─────────────────────────────────────────────┘
```

### 3. **정렬 방식**

API 목록은 다음 순서로 자동 정렬됩니다:

1. **경로 기준** (알파벳 순)
   - `/api/products`
   - `/api/users`
   - `/api/users/{id}`

2. **같은 경로 내에서 메서드 순서**
   - GET → POST → PUT → PATCH → DELETE

---

## 📈 추가된 정보

### 프로젝트 정보 카드

**Before:**
```
┌────────────┬────────────┬────────────┐
│마지막 체크 │스냅샷 수   │최신 버전   │
└────────────┴────────────┴────────────┘
```

**After:**
```
┌────────────┬────────────┬────────────┬───────────┐
│마지막 체크 │스냅샷 수   │최신 버전   │총 API 개수│
│2분 전      │3개         │v1.0.0     │15개       │
└────────────┴────────────┴────────────┴───────────┘
```

---

## 🎯 장점

### 1. **한눈에 API 현황 파악**
- 변경사항이 없어도 현재 API 목록 확인 가능
- 프로젝트에 어떤 API가 있는지 즉시 파악

### 2. **메서드별 색상 구분**
- GET (파란색) - 조회
- POST (초록색) - 생성
- PUT (노란색) - 수정
- DELETE (빨간색) - 삭제

### 3. **검색 및 확인 편의성**
- 스크롤로 많은 API도 쉽게 탐색
- Monospace 폰트로 경로 명확하게 표시
- Summary로 각 API의 목적 파악

### 4. **변경 추적 맥락 제공**
- 변경 내역을 볼 때 전체 API 구조 참고 가능
- Breaking Change 영향 범위 이해

---

## 🔍 상세 화면 예시

### 실제 사용 예 (Petstore API)

```
API 목록                          총 20개 엔드포인트
┌─────────────────────────────────────────────────┐
│ [POST]   /pet                  Add a new pet    │
│ [PUT]    /pet                  Update pet       │
│ [GET]    /pet/findByStatus     Finds by status  │
│ [GET]    /pet/findByTags       Finds by tags    │
│ [GET]    /pet/{petId}          Find pet by ID   │
│ [POST]   /pet/{petId}          Updates a pet    │
│ [DELETE] /pet/{petId}          Deletes a pet    │
│ [POST]   /pet/{petId}/uploadImage Upload image  │
│ [GET]    /store/inventory      Returns inventory│
│ [POST]   /store/order          Place an order   │
│ ...                                              │
└─────────────────────────────────────────────────┘
```

---

## 📱 반응형 지원

### 데스크톱
- 2열 레이아웃 (메서드 + 정보)
- 최대 높이 500px

### 모바일
- 1열 레이아웃 (세로 스택)
- 최대 높이 400px
- 메서드 뱃지 크기 조정

---

## 🎉 완성!

이제 프로젝트 상세 화면에서:
- ✅ **프로젝트 정보** - 기본 통계
- ✅ **API 목록** - 현재 등록된 엔드포인트 (NEW!)
- ✅ **변경 내역** - Diff 카드 목록

모든 정보를 한 화면에서 확인할 수 있습니다! 🚀

**페이지를 새로고침하고 프로젝트 상세보기에서 API 목록을 확인하세요!**
