# 🔽 API 목록 토글 기능 추가

## ✅ 구현 완료

API 목록을 접었다 펼칠 수 있는 토글 기능이 추가되었습니다!

---

## 🎯 기능 요약

### 토글 기능
- **헤더 클릭**: API 목록 섹션 헤더 전체를 클릭하면 목록이 펼쳐지거나 접힘
- **토글 버튼**: 우측에 화살표 아이콘(▶/▼) 표시
- **기본 상태**: 열린 상태(expanded)로 시작

---

## 🎨 UI 변경사항

### Before (항상 열림)
```
API 목록                          총 15개 엔드포인트
┌─────────────────────────────────────────────┐
│ [GET]  /api/users           사용자 목록 조회 │
│ [POST] /api/users           새 사용자 생성   │
│ ...                                         │
└─────────────────────────────────────────────┘
```

### After (토글 가능)

**열린 상태:**
```
API 목록  총 15개 엔드포인트                    ▼
┌─────────────────────────────────────────────┐
│ [GET]  /api/users           사용자 목록 조회 │
│ [POST] /api/users           새 사용자 생성   │
│ ...                                         │
└─────────────────────────────────────────────┘
```

**닫힌 상태:**
```
API 목록  총 15개 엔드포인트                    ▶
```

---

## 💻 구현 상세

### 1. **상태 관리**

```typescript
const showApiList = ref(true) // 기본값: 열림

function toggleApiList() {
  showApiList.value = !showApiList.value
}
```

### 2. **템플릿 수정**

```vue
<div class="section-header" @click="toggleApiList">
  <div class="header-title">
    <h2>API 목록</h2>
    <span class="api-count">총 {{ apiEndpoints.length }}개 엔드포인트</span>
  </div>
  <button class="toggle-btn" :class="{ expanded: showApiList }">
    {{ showApiList ? '▼' : '▶' }}
  </button>
</div>

<div v-else-if="showApiList" class="api-list">
  <!-- API 목록 표시 -->
</div>
```

### 3. **스타일링**

```scss
.section-header {
  cursor: pointer;              // 클릭 가능 표시
  user-select: none;            // 텍스트 선택 방지
  transition: background 0.2s;   // 호버 효과

  &:hover {
    background: var(--bg-tertiary);  // 호버 시 배경색 변경
  }
}

.toggle-btn {
  font-size: 1rem;
  color: var(--color-text-secondary);
  transition: all 0.2s;

  &:hover {
    color: var(--color-text-primary);
    transform: scale(1.1);        // 확대 효과
  }

  &.expanded {
    color: var(--color-primary);   // 열린 상태는 파란색
  }
}
```

---

## 🎮 사용 방법

### 토글 방법

**1. 헤더 전체 클릭**
```
[클릭] → API 목록  총 15개 엔드포인트  ▼
                                    ↑
                            목록 접기/펼치기
```

**2. 화살표 버튼 클릭**
```
API 목록  총 15개 엔드포인트  [클릭 ▶]
                              ↑
                          직접 클릭 가능
```

---

## ✨ UX 특징

### 1. **시각적 피드백**

- **호버 효과**: 헤더에 마우스 오버 시 배경색 변경
- **화살표 색상**: 열린 상태(▼)는 파란색, 닫힌 상태(▶)는 회색
- **화살표 확대**: 버튼 호버 시 10% 확대

### 2. **직관적인 인터랙션**

- **전체 헤더 클릭 가능**: 헤더 어디든 클릭하면 토글
- **시각적 상태 표시**: 화살표로 현재 상태 명확히 표시
- **부드러운 전환**: 모든 상태 변화에 transition 적용

### 3. **접근성**

- `cursor: pointer`: 클릭 가능함을 표시
- `user-select: none`: 텍스트 드래그 방지
- 명확한 시각적 피드백

---

## 🎯 토글 상태

### 기본 상태: 열림 (Expanded)
```typescript
const showApiList = ref(true)
```

**이유:**
- 사용자가 처음 방문 시 API 목록을 바로 확인 가능
- 프로젝트 정보와 함께 전체 현황 파악 용이

### 상태 변경: 클릭으로 전환
- **열림(▼)** → 클릭 → **닫힘(▶)**
- **닫힘(▶)** → 클릭 → **열림(▼)**

---

## 📊 화면 예시

### 열린 상태 (기본)

```
┌─────────────────────────────────────────────────┐
│ API 목록  총 15개 엔드포인트                  ▼ │ ← 클릭 가능
├─────────────────────────────────────────────────┤
│ [GET]  /api/users           사용자 목록 조회     │
│ [POST] /api/users           새 사용자 생성       │
│ [GET]  /api/users/{id}      사용자 상세 조회     │
│ [PUT]  /api/users/{id}      사용자 정보 수정     │
│ [DELETE] /api/users/{id}    사용자 삭제         │
│ [GET]  /api/products        상품 목록 조회       │
│ ...                                             │
└─────────────────────────────────────────────────┘
```

### 닫힌 상태

```
┌─────────────────────────────────────────────────┐
│ API 목록  총 15개 엔드포인트                  ▶ │ ← 클릭 가능
└─────────────────────────────────────────────────┘
```

---

## 🚀 장점

### 1. **공간 효율성**
- 필요 없을 때는 접어서 화면 공간 절약
- 변경 내역에 집중할 때 유용

### 2. **선택적 확인**
- 사용자가 필요한 정보만 표시
- 스크롤 양 감소

### 3. **깔끔한 UI**
- 정보 과부하 방지
- 원하는 정보에 빠르게 접근

---

## 🎨 호버 효과

```scss
헤더 호버:
- 배경색: 투명 → 회색 (var(--bg-tertiary))

버튼 호버:
- 색상: 회색 → 검은색
- 크기: 1.0 → 1.1 (10% 확대)

열린 상태 버튼:
- 색상: 파란색 (var(--color-primary))
```

---

## 📱 반응형 지원

모바일과 데스크톱 모두에서 동일하게 작동:
- 터치로 토글
- 호버 효과는 데스크톱에서만

---

## 🎉 완성!

이제 API 목록을 자유롭게 접었다 펼칠 수 있습니다!

**사용 방법:**
1. **헤더 클릭** → 목록 토글
2. **화살표 확인** → 현재 상태 파악
   - **▼** = 열림
   - **▶** = 닫힘

**페이지를 새로고침하고 API 목록 헤더를 클릭해보세요!** 🚀
